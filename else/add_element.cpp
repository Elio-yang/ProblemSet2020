#include <stdio.h>
#include <stdlib.h>

#define data_t int

struct node{
    data_t key;
    struct node* next;
};

struct node *insert(struct node *node);
struct node *find(struct node *node);
int main()
{


    return 0;
}

struct node *insert(struct node* node)
{

}